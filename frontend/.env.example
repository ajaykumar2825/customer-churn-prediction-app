# Public URL the browser uses to reach the FastAPI service.
# Default works for native and docker-compose (host port publishing).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Public URL of the frontend itself (used for metadata / OG tags).
NEXT_PUBLIC_APP_URL=http://localhost:3000